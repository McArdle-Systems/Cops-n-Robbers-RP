// Placeholder mission config. The real Mission Header + ScenarioFramework
// scaffold needs to be authored in the Workbench (it generates GUIDs and
// references the GameMode prefab). Steps to wire up the POC inside the
// Workbench:
//
// 1. Right-click Missions/ in Resource Browser -> New -> Mission Header.
// 2. Set "World" to a small test map (Everon -> St. Pierre area is ideal).
// 3. Duplicate any small scenario's GameMode prefab; on the duplicate,
//    add the RP_AIMovePOCComponent and fill in its prefab attributes.
// 4. Set the GameMode prefab on the Mission Header.
// 5. Save and Play.
//
// This file is kept as a TODO marker so the directory is tracked.
